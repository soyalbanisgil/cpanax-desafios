import styled from 'styled-components';

export const FlowContainer = styled.div`
  height: 94vh;
  position: relative;
  .btn {
    position: absolute;
    top: 2rem;
    right: 2rem;
    font-size: 1.5rem;
    padding: 1rem;
    &:hover {
      cursor: pointer;
    }
  }
`;
